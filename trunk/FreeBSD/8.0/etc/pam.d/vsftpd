auth		required	/usr/local/lib/pam_pwdfile.so pwdfile /usr/local/etc/vsftpd.passwd

account		required        pam_nologin.so
account		required        pam_unix.so

session		required        pam_permit.so
